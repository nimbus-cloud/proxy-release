# abort script on any command that exits with a non zero value
set -e -x

echo "Extracting squid...."

tar xvzf squid/squid-3.4.6.tar.gz
pushd squid-3.4.6
  ./configure \
    --prefix=${BOSH_INSTALL_TARGET}
  make
  make install
popd

